# Codex 开发规范

## 工作流

1. 阅读相关需求、API 和现有代码。
2. 用最小变更完成一个可验证功能。
3. 运行对应构建或测试命令。
4. 记录变更点、验证结果和遗留问题。

## 任务边界

- 新功能必须包含接口、前端入口和文档更新。
- Bug 修复必须包含复现条件和验证命令。
- AI 提示词变更必须保留输入输出 JSON 示例。

## 安全约束

- 不提交 `.env`。
- 不提交真实 API Key。
- 不在代码里写死本机绝对路径，部署路径写入文档。

