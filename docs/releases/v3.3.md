# v3.3 One-Command Deployment and Distributed AI Flow

## Scope

- Added `scripts/deploy-three-vm.ps1` as the Windows host entry for one-command three-VM deployment.
- Added `scripts/check-distributed-ai-flow.ps1` to verify the end-to-end flow from delivery creation to RocketMQ event publishing, AI event consumption, async screening task completion, and real DashScope candidate screening.
- Fixed local dev startup by replacing the broken parent-POM `spring-boot:run` flow with a build-once, run-jar startup script.
- Fixed RocketMQ broker advertisement for cross-VM clients by generating `brokerIP1=${VM3_HOST}` inside `deploy/docker-compose.vm3.yml`.
- Fixed the deploy script SSH command binding by using `-RemoteCommand` consistently, and resolved VMX files from VM directories to avoid depending on a non-ASCII VMX filename literal.
- Fixed three-VM startup ordering by bootstrapping VM1 Nacos before VM3 `ai-service` and VM2 business services.
- The three-VM deploy script removes stale `recruit-*` containers by default, which is required when VM2 and VM3 were cloned from VM1 and still have VM1 Nacos/Gateway containers occupying ports.

## Deployment Entry

Interactive SSH password mode:

```powershell
.\scripts\deploy-three-vm.ps1 -SshUser ubuntu
```

SSH key mode:

```powershell
.\scripts\deploy-three-vm.ps1 -SshUser ubuntu -IdentityFile $HOME\.ssh\id_ed25519
```

Non-interactive validation mode:

```powershell
.\scripts\deploy-three-vm.ps1 -SshUser ubuntu -IdentityFile $HOME\.ssh\id_ed25519 -NonInteractive
```

The script resolves VM IPs with `vmrun`, builds a temporary deployment env without printing secrets, uploads the committed Git revision to each VM, starts VM1 Nacos first, then starts VM3 -> VM2 -> VM1 and runs the health and distributed AI flow checks.

## Distributed AI Flow Verification

Local verification was run with real RocketMQ containers and the real configured DashScope key:

```powershell
.\scripts\check-distributed-ai-flow.ps1 `
  -BaseUrl http://localhost:18080 `
  -AiBaseUrl http://localhost:8106 `
  -TimeoutSeconds 30 `
  -TaskTimeoutSeconds 240 `
  -AiAttempts 3
```

Report:

- `reports/deploy/distributed-ai-flow-20260611-170537.md`

Result:

- `PASS=5`
- `FAIL=0`

Confirmed:

- Gateway demo login issued student and company tokens.
- DashScope status returned `configured=true`.
- Real candidate screening returned `mocked=false`.
- Delivery creation published a RocketMQ event.
- `ai-service` consumed the event and produced exactly one completed `ROCKETMQ` screening task for the delivery.

## Three-VM Verification Status

Latest VMware Tools IPs:

- VM1: `192.168.6.130`
- VM2: `192.168.6.141`
- VM3: `192.168.6.142`

Latest smoke report:

- `reports/deploy/three-vm-smoke-20260611-170501.md`

Result:

- `PASS=40`
- `FAIL=0`
- `SKIPPED=0`

Confirmed:

- All three VMs are running and reachable by ping.
- All three Compose files validate locally with `docker compose config --quiet`.
- VM1 exposes frontend, Gateway, Nacos HTTP, and Nacos gRPC.
- VM2 exposes the six business services on `8101`, `8102`, `8103`, `8104`, `8105`, and `8107`.
- VM3 exposes `ai-service`, MySQL, Redis, MinIO, and RocketMQ ports.
- Frontend `/api/ai/status` and Gateway `/api/ai/status` both return HTTP 200.

## Real Three-VM Deployment

The Windows host SSH key was installed into the Ubuntu VM user account so the deployment script can run non-interactively without storing passwords in the repository.

Successful verification command:

```powershell
.\scripts\deploy-three-vm.ps1 `
  -EnvFile .\.env `
  -SshUser namettr `
  -IdentityFile C:\Users\G5080\.ssh\id_ed25519 `
  -Vm1Ip 192.168.6.130 `
  -Vm2Ip 192.168.6.141 `
  -Vm3Ip 192.168.6.142 `
  -DemoPassword 123456 `
  -TimeoutSeconds 30 `
  -DisableConnectionSharing
```

The command completed with three-VM smoke `PASS=40, FAIL=0, SKIPPED=0` and distributed AI flow `PASS=5, FAIL=0`.

## Verification

```powershell
$files = @(
  ".\scripts\start-local-dev.ps1",
  ".\scripts\deploy-three-vm.ps1",
  ".\scripts\check-distributed-ai-flow.ps1"
)
foreach ($file in $files) {
  $tokens = $null
  $errors = $null
  [System.Management.Automation.Language.Parser]::ParseFile(
    (Resolve-Path $file),
    [ref]$tokens,
    [ref]$errors
  ) | Out-Null
  if ($errors.Count -gt 0) { $errors; exit 1 }
}

docker compose --env-file deploy\three-vm.env.example -f deploy\docker-compose.vm3.yml config --quiet
.\scripts\deploy-three-vm.ps1 -DryRun -NonInteractive -SkipSmoke -SkipDistributedAiFlow
```
